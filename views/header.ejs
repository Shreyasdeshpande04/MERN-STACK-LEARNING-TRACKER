<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Professional Navbar</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />

  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    nav {
      background-color: #ffffff;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 30px;
      height: 60px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      position: sticky;
      top: 0;
      z-index: 1000;
      flex-wrap: nowrap;
      box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.2);

      /* Text shadow */
      text-shadow: 1px 1px 1px rgba(0, 0, 0, 0.2);
    }

    .nav-left {
      display: flex;
      align-items: center;
      gap: 20px;
    }

    .brand-title {
      font-size: 18px;
      font-weight: bold;
      color: #000;
      white-space: nowrap;
    }

    #navList {
      list-style: none;
      display: flex;
      gap: 20px;
    }

    #navList li a {
      text-decoration: none;
      color: black;
      padding: 10px 15px;
      border-radius: 5px;
      font-weight: 500;
      transition: background 0.3s ease;
      position: relative; /* Needed for positioning the pseudo-element */
      overflow: hidden; /* Hide the pseudo-element initially */
      display: block; /* Ensure the padding works correctly */
    }

    /* Base style for the colored strip (hidden by default) */
    #navList li a::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      width: 0; /* Starts with zero width */
      height: 100%;
      background-color: transparent; /* Default */
      transition: width 0.3s ease; /* Animate the width */
      z-index: -1; /* Behind the text */
      border-radius: 5px; /* Match the link's border-radius */
    }

    /* Different colors for each menu item's strip */
    #navList li:nth-child(1) a::before { /* Home */
      background-color: #4CAF50; /* Green */
    }
    #navList li:nth-child(2) a::before { /* Post/MERN */
      background-color: #2196F3; /* Blue */
    }
    #navList li:nth-child(3) a::before { /* Table/Contact */
      background-color: #FFC107; /* Amber */
    }
    #navList li:nth-child(4) a::before { /* MERN/Login */
      background-color: #F44336; /* Red */
    }
    #navList li:nth-child(5) a::before { /* Contact/-- */
      background-color: #9C27B0; /* Purple */
    }
    #navList li:nth-child(6) a::before { /* Logout/-- */
        background-color: #00BCD4; /* Cyan */
    }


    /* Hover effect for the strip */
    #navList li a:hover::before {
      width: 100%; /* Expand to full width on hover */
    }

    /* Keep the text white on hover over the strip */
    #navList li a:hover {
      color: white; /* Change text color on hover */
    }

    .hamburger {
      display: none;
      font-size: 24px;
      cursor: pointer;
      color: black;
    }

    @media (max-width: 768px) {
      nav {
        flex-direction: column;
        align-items: flex-start;
        height: auto;
        padding: 10px 20px;
      }

      .hamburger {
        display: block;
        align-self: flex-end;
        margin-bottom: 5px;
      }

      #navList {
        display: none;
        flex-direction: column;
        width: 100%;
        margin-top: 10px;
      }

      #navList.show {
        display: flex;
        background-color: #FFDCDC;
      }

      #navList li a {
        padding: 12px 20px;
        width: 100%;
      }

      /* Adjust for mobile menu, the strip should still work */
      #navList li a::before {
        border-radius: 0; /* No rounded corners for full width in mobile */
      }

      .brand-title {
        margin-bottom: 5px;
      }
    }
  </style>
</head>
<body>

  <nav>
    <div class="nav-left">
      <div class="brand-title">MY MERN STACK JOURNEY</div>
    </div>

    <div class="hamburger" onclick="toggleMenu()"><i class="fas fa-bars"></i></div>

    <ul id="navList">
      <li><a href="/homepage"><i class="fas fa-home"></i> Home</a></li>
      <% if (loggedIn) { %>
        <li><a href="/signup"><i class="fas fa-plus-circle"></i> Post</a></li>
        <li><a href="/list"><i class="fas fa-table"></i> Table</a></li>
        <li><a href="/services"><i class="fas fa-cogs"></i> MERN</a></li>
        <li><a href="/contact"><i class="fas fa-envelope"></i> Contact</a></li>
        <li><a href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a></li>
      <% } else { %>
        <li><a href="/services"><i class="fas fa-cogs"></i> MERN</a></li>
        <li><a href="/contact"><i class="fas fa-envelope"></i> Contact</a></li>
        <li><a href="/login"><i class="fas fa-sign-in-alt"></i> Login</a></li>
      <% } %>
    </ul>
  </nav>

  <script>
    function toggleMenu() {
      document.getElementById("navList").classList.toggle("show");
    }
  </script>

</body>
</html>